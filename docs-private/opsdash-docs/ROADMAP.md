# Roadmap (short)

## P0 (now)
- Balance config simplification + `/overview/persist` payload sanity.
- Core test coverage: targets summary/pace helpers, chart helpers, keyboard shortcuts.

## P1 (next)
- UI polish for charts and layout tabs.
- Frontend bundle-size reduction pass: analyze the main Vite chunk, split heavy chart/onboarding/edit-mode code, and keep the warning as a real performance signal until the main bundle is smaller.

## P2 (later)
- Caching review follow-ups.
- i18n expansion and accessibility sweep.
- Delivery pipeline for reporting (email/activity).
